# 디자인 방향 (Design Direction)

> plan.md의 5원칙을 구체화한 디자인 스펙 초안. 개발 전 확정 필요.

## 1. 컨셉 키워드

**"Clean Terminal"** — 조용한 배경 위에 정돈된 타이포그래피, 터미널/코드에서 따온 포인트 요소.

- 배경은 뉴트럴 톤, 채도 낮은 포인트 컬러 1개만 사용 → 심플/모던 (N-01, N-05)
- 모노스페이스 폰트와 얇은 보더 라인으로 테크니컬한 느낌 (N-04)

## 2. 컬러 팔레트 (라이트 테마 기준, 확정 전 제안)

| 역할 | 색상 | 비고 |
|---|---|---|
| 배경 | `#FAFAFA` / `#FFFFFF` | 카드형 요소는 흰색 |
| 본문 텍스트 | `#1C1E21` | 거의 검정, 부드러운 톤 |
| 보조 텍스트 | `#6B7280` | 날짜, 카테고리 등 |
| 포인트 컬러 | `#2563EB` (파랑 계열) | 링크, 강조, 버튼 — 대안: `#0EA5E9`, `#6366F1` |
| 보더/구분선 | `#E5E7EB` | 1px 라인 |
| 코드 블록 배경 | `#1E1E2E` (다크) | 본문과 명확히 대비 → 테크니컬 느낌 |
| 코드 강조 | VS Code 다크 테마 계열 | One Dark / Catppuccin 계열 |

> 다크 모드(F-09) 도입 시: 배경 `#111318`, 텍스트 `#E5E7EB`, 포인트 컬러 유지.

## 3. 타이포그래피

| 용도 | 폰트 | 크기/스타일 |
|---|---|---|
| 본문 | Pretendard (시스템 폴백 포함) | 16~17px / line-height 1.75 |
| 제목(h1~h4) | Pretendard Bold | 본문 1.5~2.5배, 계층 명확히 |
| 코드 | JetBrains Mono 또는 D2Coding | 14px / line-height 1.6 |
| UI(메뉴 등) | Pretendard | 14~15px, letter-spacing 살짝 |

- 한국어 본문 기준 넉넉한 자간·행간으로 가독성 확보 (N-03).
- 글 제목은 크고 단정하게, 날짜/카테고리 메타는 작게+회색.

## 4. 레이아웃

### 데스크톱 (≥1024px)
- 최대 폭 1100~1200px 중앙 정렬, **헤더(로고+메뉴) + 본문 중심 1단**.
- **사이드바(280px) 토글 방식 (확정)**: 헤더의 토글 버튼으로 접었다 펼 수 있음.
  접으면 본문이 전체 폭으로 확장. 토글 상태는 localStorage에 저장해 유지.
- 본문 상/하단 광고: 본문 폭에 맞춰 중앙 배치 (728x90 반응형).

### 태블릿 (768~1023px)
- 사이드바 숨김(드로어로), 본문 단일 컬럼.

### 모바일 (<768px)
- 햄버거 메뉴 → 슬라이드 드로어(카테고리, 검색, 사이드바 위젯).
- 본문 여백 20px, 광고는 반응형 유닛 그대로.

### 반응형 브레이크포인트
```
360 / 768 / 1024 / 1280  (Mobile-first)
```

## 5. 주요 UI 요소 방향

| 요소 | 방향 |
|---|---|
| 글 목록 | 썸네일 + 제목 + 요약(2줄) 카드형, hover 시 보더 강조 |
| 목록 페이징 | 숫자형, 현재 페이지 포인트 컬러 |
| 코드 블록 | 다크 배경 + 언어 태그 + 복사 버튼(선택) |
| 태그 | 필터 칩 형태, 모노스페이스 소문자 |
| 댓글 | 평면형, 아바타 원형, 여백 넉넉히 |
| 헤더 | 스크롤 시 블러 배경 sticky (선택) |
| 방문자수/위젯 | 사이드바 카드 안에 접어서 심플하게 |

## 6. 광고 배치 (F-02 보장)

- `[##_revenue_list_upper_##]` : 헤더 직후/목록 위 — 상단 배너.
- `[##_revenue_list_lower_##]` : 목록 하단.
- 글 화면 본문 상/하단 광고는 애드센스 설정 위치에 맞춰 컨테이너만 스타일링.
- 광고 영역은 본문과 시각적으로 분리하되 과하지 않게 (테두리 없이 여백으로).
- **치환자 삭제/중복 금지** — 마크업 감싸기만.

## 7. 결정 사항 현황 (2026-09-04 업데이트)

| 항목 | 상태 |
|---|---|
| 사이드바 | **확정 — 토글 방식**: 데스크톱은 헤더 버튼으로 접기/펼치기, 모바일은 드로어 |
| 목업 | `mockup.html` 작성 완료 → **사용자 브라우저 확인 후 피드백 대기** |
| 홈 광고 | **제외 확정** (2026-09-04) — 홈은 광고 없는 풀스크린 히어로, 광고는 글 화면 중심 |
| 카테고리 재편 | 스킨 적용 후 카테고리 구조를 다시 변경할 예정 → 스킨은 카테고리 개수/이름에 독립적이어야 함 (치환자 기반, 하드코딩 금지) |
| 홈 커버 | 미확정 |
| 다크 모드 | **목업 토글 구현 — 검수 중** (헤더 🌙 토글, localStorage 저장, 변수 오버라이드 방식) |
| 포인트 컬러 | 파랑 계열(#2563EB) 제안 — 목업 피드백으로 확정 |

> **반응형 확인 방법**: `mockup.html`을 브라우저에서 열고 창 크기를 조절하거나
> 개발자도구(F12) 기기 에뮬레이션(모바일/태블릿)으로 레이아웃이 안 깨지는지 확인.

## 8. 홈 화면 인터랙션 (2026-09-04 2차 피드백 반영 — 풀스크린 히어로)

anime.js 3.x 기반, **animejs.com 랜딩 스타일**. 홈은 광고 없이 풀스크린 단일 화면으로 시작.

| 요소 | 효과 |
|---|---|
| 히어로 레이아웃 | **뷰포트 풀스크린** (header+탭 제외 100vh), 홈에서 광고·사이드바 미노출 |
| 배경 | 다크 그라디언트 + 글로우 블록 3개 드리프트 + 그리드 라인(mask 페이드) |
| 플로팅 요소 | 코드 심볼(`{ }`, `=>`, `;`, `#include` 등) 부유 애니메이션 (모바일에서 숨김) |
| 텍스트 | 태그 → 대형 타이틀(라인 스태거) → 타이핑 → CTA → 통계 순차 등장 |
| CTA 버튼 | "최신 글 보기"(부드러운 스크롤) / "GitHub" 고스트 버튼, hover 리프트 |
| 통계 숫자 | 카운트업 (easeOutExpo) |
| 글 카드 | 히어로 아래로 스크롤 시 IntersectionObserver로 화면 진입 시 스태거 등장 |
| 스크롤 전환 (터미널 컨셉) | 휠을 **조금씩** 돌리면 히어로가 살짝 떠오르고(누적 게이지) 하단에 터미널 프롬프트 노출. 누적 델타 **240**(≈휠 2노치)을 넘기면 전환 연출 시작. 300ms 무입력 시 부분 이동 복원 |
| 전환 연출 (아래) | 터미널에 `cd ./posts` 타이핑 → 타이틀 글자 **산산조각** 분산(easeInExpo) + 1.15s 순항으로 목록 진입 → 카드 스태거 등장 |
| 전환 연출 (위) | **목록 첫 화면 근처(리스트 상단 기준 위로 120px 이내)에서 위로 휠 누적** → 터미널에 `cd ..` 타이핑 → 카드 페이드아웃 + 위로 순항 → 히어로 **재조립**(easeOutBack). 목록 첫 카드를 보다가 위로 돌리면 자동 발동 |
| 전환 연출 (위) | (양방향 지원 — 위/아래 동일한 터미널·파괴/재조립 컨셉) |
| 마우스 패럴랙스 | 히어로 코어가 커서 방향으로 미세 이동 (mouseleave 복귀) |
| 접근성 | `prefers-reduced-motion` 시 애니메이션 생략, 타이핑만 유지 |
| 폴백 | anime.js 미로딩 시에도 콘텐츠 정상 표시 |

> - 사이드바 토글은 글/목록 화면에서 동작 (홈에서는 레이아웃 자체 미노출).
> - Tistory 적용 시 `revenue_list_upper/lower` 치환자는 그대로 두고, 홈(`tt-body-index`)에서는 CSS로 광고 영역 숨김 처리.
> - 실제 스킨 적용 시: anime.min.js는 스킨 `images/` 폴더에 넣어 로컬 로드 권장(CDN 의존 제거).
